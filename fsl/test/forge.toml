[package]
name = "forge:test"
version = "0.1.0-pre-alpha"
description = "Built-in test runner: component testing, server function testing, snapshots, coverage"
license = "AGPL-3.0-only"

[dependencies]
