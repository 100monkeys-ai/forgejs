[package]
name = "forge:email"
version = "0.1.0-pre-alpha"
description = "Transactional email: .fx templates compiled to HTML, SMTP/Resend/Postmark adapters, preview server"
license = "AGPL-3.0-only"

[dependencies]
