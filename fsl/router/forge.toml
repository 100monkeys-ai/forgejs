[package]
name = "forge:router"
version = "0.1.0-pre-alpha"
description = "First-party routing for Forge applications: file-based and explicit routes, nested layouts, typed params, middleware"
license = "AGPL-3.0-only"

[dependencies]
